import { MinLowercaseValidatorDirective } from './min-lowercase.directive';

describe('Directive: MinLowercase', () => {
  it('should create an instance', () => {
    const directive = new MinLowercaseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
