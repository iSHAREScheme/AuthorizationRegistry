import { MinNumericValidatorDirective } from './min-numeric.directive';

describe('Directive: MinNumeric', () => {
  it('should create an instance', () => {
    const directive = new MinNumericValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
