import { MinSpecialCharsValidatorDirective } from './min-special-characters.directive';

describe('Directive: MinSpecialCharacter', () => {
  it('should create an instance', () => {
    const directive = new MinSpecialCharsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
