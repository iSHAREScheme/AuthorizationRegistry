import { MinUppercaseValidatorDirective } from './min-uppercase.directive';

describe('Directive: MinUppercase', () => {
  it('should create an instance', () => {
    const directive = new MinUppercaseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
